<window
    id="desktop"
    x="0"
    y="0"
    width="${screen.width}"
    height="${screen.height}"
    type="background"
    border="none"
    allowClose="false">

    <panel dock="fill">
        <image src="res.loadImage('${desktop.wallpaper}')" dock="fill" layer="1"/>
    </panel>
</window>

<window
    id="mixer"
    x="${mixer.x}"
    y="${mixer.y}"
    width="${mixer.width}"
    height="${mixer.height}"
    type="topMost"
    border="none"
    autoShow="false">

    <externalProcess fileName="SndVol.exe" titleMatch="Микшер" dock="fill"/>
</window>
